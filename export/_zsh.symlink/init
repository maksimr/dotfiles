# Load scripts
# vim: ft=zsh

function {
  local scripts
  scripts=(
    ~/.nvm/nvm.sh
    ~/.dvm/dvm.sh
  )

  for src in $scripts
  do
    # XXX optimization loading nvm.sh
    # 10ms loading nvm.sh
    # & - optimize loading (send loading script to background)
    . $src 2>/dev/null &
  done
}
